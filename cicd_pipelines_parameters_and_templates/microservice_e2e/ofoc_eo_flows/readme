This template generates an e2e microservice flow for the OFOC applications.
The reason this is separated is because OFOC is an EO specific application.

These microservices can be taken into the following products:
  EO